name: "Training Log"
description: "Register a daily training log in JSON format"
title: "Training Log"
labels: ["training-log"]
body:
  - type: textarea
    id: log
    attributes:
      label: "Training Log JSON"
      description: "Paste the username followed by the log JSON, e.g. `kenta{...}`"
    validations:
      required: true
